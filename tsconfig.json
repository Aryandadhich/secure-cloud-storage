{
  "compilerOptions": {
    /* Language & Environment */
    "target": "es2016",            // Use modern JavaScript features for compiled output
    "module": "CommonJS",            // Use ES module system (import/export) instead of CommonJS
    "moduleResolution": "node",    // Resolve modules like Node.js does (important for Express, etc.)

    /* Project Structure */
    "rootDir": "./src",            // Where TypeScript source files are located
    "outDir": "./dist",            // Where compiled JavaScript files will be stored after build

    /* Interop & Compatibility */
    "esModuleInterop": true,       // Allows default imports for CommonJS modules like 'express'
    "forceConsistentCasingInFileNames": true, // Prevent issues on case-sensitive OS (Linux)

    /* Type Safety */
    "strict": true,                // Enable all strict type checking options (best practice)
    "skipLibCheck": true           // Skip type checking for .d.ts files (faster build)
  },

  /* Files to Include & Exclude */
  "include": ["src"],              // Compile only files inside src folder
  "exclude": ["node_modules", "dist"] // Don't compile node_modules or already built JS files
}
